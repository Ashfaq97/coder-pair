version: "3.9"
services:
  coder-pair-db:
    image: postgres
    restart: always
    container_name: coder-pair-db-container
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: example
      PGDATA: /data/postgres
    volumes:
      - postgres:/data/postgres

volumes:
  postgres: